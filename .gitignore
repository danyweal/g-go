
############################
# Node / Next.js
############################

node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Next.js build output
.next/
out/
dist/
build/

# Next.js cache
.next/cache/

############################
# Environment variables
############################

# Root env files (DO NOT COMMIT SECRETS)
.env
.env.local
.env.development
.env.test
.env.production
.env.*.local
.env.*

# Env files in subfolders (e.g. Firebase functions)
functions/.env
functions/.env.local
functions/.env.development
functions/.env.test
functions/.env.production
functions/.env.*.local
functions/.env.*

############################
# Firebase / Google Cloud
############################

# Local Firebase cache / state
.firebase/
firebase-debug.log

# Service account / credentials JSON
# (one of these was the blocked file: pcnw-app-0daadb04c541.json)
pcnw-app-*.json
*serviceAccount*.json
*service-account*.json
*service_account*.json
*credentials*.json
*keyfile*.json
*key-file*.json

# Keep Firebase config files tracked:
# (make sure these files exist in your repo and are NOT ignored)
!firebase.json
!.firebaserc
!firestore.rules
!firestore.indexes.json
!storage.rules

############################
# Logs
############################

logs/
*.log
log.txt
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

############################
# OS / Editor junk
############################

# macOS
.DS_Store

# Windows
Thumbs.db
Desktop.ini

# VS Code / IDE
.vscode/
.idea/
*.swp
*.swo

############################
# Misc build / cache
############################

.cache/
coverage/
